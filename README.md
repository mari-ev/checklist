**Отчёт по тестам для системы управления книгами (BooksCollector)**


**Всего тестов:** 10.


### 1. Тесты про добавление книг


**Тест в коде:** `test_add_new_book_valid_name`

* **Что проверяем:** может ли система добавить книгу с обычным названием.
* **Как тестируем:** пытаемся добавить книгу «Сияние».
* **Что должно произойти:** книга появляется в коллекции, но пока без жанра.

**Тест в коде:** `test_add_new_book_invalid_name` (параметризованный)

* **Что проверяем:** как система реагирует на странные названия.
* **Какие названия тестируем:**
    * очень длинное (41 символ);
    * пустое (ничего не написано).
* **Что должно произойти:** система не добавляет такие книги — они не появляются в коллекции.

**Тест в коде:** `test_no_duplicates_in_collection`

* **Что проверяем:** не появится ли в коллекции две одинаковые книги.
* **Как тестируем:** дважды пытаемся добавить одну и ту же книгу («Воспламеняющая взглядом»).
* **Что должно произойти:** в коллекции остаётся только одна копия книги.

### 2. Тесты про жанры книг

**Тест в коде:** `test_set_book_genre_valid_genre`

* **Что проверяем:** можно ли присвоить книге существующий жанр.
* **Как тестируем:** добавляем книгу «Оно» и ставим ей жанр «Ужасы».
* **Что должно произойти:** система запоминает, что «Оно» — это ужасы.

**Тест в коде:** `test_set_book_genre_invalid_genre`

* **Что проверяем:** что будет, если выбрать жанр, которого нет в списке.
* **Как тестируем:** пытаемся поставить книге «Мизери» жанр «Романтика».
* **Что должно произойти:** жанр не присваивается, книга остаётся без жанра.

**Тест в коде:** `test_get_books_with_specific_genre`

* **Что проверяем:** умеет ли система находить книги определённого жанра.
* **Как тестируем:**
    * добавляем «Тёмную башню» и ставим жанр «Фантастика»;
    * добавляем «Зелёную милю» и ставим жанр «Классика»;
    * просим показать все книги жанра «Фантастика».
* **Что должно произойти:** система показывает только «Тёмную башню».

### 3. Тесты про детские книги

**Тест в коде:** `test_get_books_for_children`

* **Что проверяем:** правильно ли система отличает детские книги от взрослых.
* **Как тестируем:**
    * добавляем «Куджо» с жанром «Мультфильмы» (детская);
    * добавляем «Оно» с жанром «Ужасы» (не детская).
* **Что должно произойти:** «Куджо» попадает в список детских книг, а «Оно» — нет.

**Тест в коде:** `test_get_books_for_children_book_with_no_genre`

* **Что проверяем:** попадёт ли в список детских книга, у которой вообще нет жанра.
* **Как тестируем:** добавляем книгу без указания жанра.
* **Что должно произойти:** такая книга не считается детской и не попадает в список.

**Тест в коде:** `test_get_books_for_children_all_books_have_adult_genres`

* **Что проверяем:** что вернёт система, если все книги в коллекции не подходят детям.
* **Как тестируем:** добавляем «Оно» (Ужасы) и «Шерлок Холмс» (Детективы).
* **Что должно произойти:** система возвращает пустой список — детских книг нет.


### 4. Тест про избранное

**Тест в коде:** `test_add_book_in_favorites`

* **Что проверяем:** можно ли добавить книгу в список избранного.
* **Как тестируем:** добавляем книгу «Противостояние» в избранное.
* **Что должно произойти:** книга появляется в списке избранного, и мы можем её там найти.

---

## Краткий итог

**Проверенные методы класса `BooksCollector`:**
* `add_new_book` — добавление книг;
* `set_book_genre` — установка жанров;
* `get_book_genre` — получение жанра книги;
* `get_books_with_specific_genre` — поиск книг по жанру;
* `get_books_for_children` — отбор детских книг;
* `add_book_in_favorites` — добавление в избранное;
* `get_list_of_favorites_books` — получение списка избранного.


**Результат:** все 10 тестов прошли успешно (`PASSED`). Система корректно выполняет все основные функции:
* добавление книг (включая обработку ошибок и дубликатов);
* установку и фильтрацию по жанрам;
* отбор детских книг (разные сценарии);
* работу с избранным.